import React from 'react';
import ComingSoon from '../components/ComingSoon';

const Settings = () => {
  return (
    <ComingSoon 
      title="Settings"
      description="Application settings, user preferences, data refresh options, and customization features are being developed."
      backLink="/"
      backText="Back to Dashboard"
    />
  );
};

export default Settings;
